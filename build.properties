# =============================================================================
# Copyright � 2021 Typefi Systems. All rights reserved.
#
# Unless required by applicable law or agreed to in writing, software
# is distributed on an "as is" basis, without warranties or conditions
# of any kind, either express or implied.
# =============================================================================

job.dir = ${basedir}/build
build.dir = ${job.dir}/Unzipped Resources
dist.dir = ${basedir}/dist
data.dir = ${basedir}/data
# to be overridden in local.properties
input.subdir =SCReports1
input.dir = ${basedir}/data/in
test.dir = ${data.dir}/test
src.dir = ${basedir}/src
lib.dir = ${basedir}/lib
resources.dir = ${basedir}/resources
development.resources.dir = ${resources.dir}/development

# to be overridden in local.properties
job.type=

build.project.name = excel2cxml
build.file.pattern = $${build.project.name}-$${cur.date}

deliverable.zip.extension = .xzip

debug = true

env.BUILD_NUMBER = local
build.number = ${env.BUILD_NUMBER}